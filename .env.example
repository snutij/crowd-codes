# Environment Variables for crowd-codes
# Copy this file to .env and fill in the values
# NEVER commit .env to version control

# YouTube Data API v3
# Get your API key from: https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=your_youtube_api_key_here

# Google Gemini API (LLM fallback for parsing)
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# GitHub Token (for creating PRs with regex suggestions)
# Create at: https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories)
GITHUB_TOKEN=your_github_token_here

# Optional: Site hostname for sitemap generation (defaults to crowd-codes.pages.dev)
# SITE_HOSTNAME=https://your-fork.pages.dev

# Optional: Discord/Slack webhook for build failure notifications
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
